const express = require('express');
const bcrypt = require('bcrypt');
const jwt = require('jsonwebtoken');

// Controller function to render the create post page
const renderCreatePostPage = (req, res) => {
    // Add your logic here to render the create post page
};

// Controller function to handle post creation
const createPost = (req, res) => {
    // Add your logic here to handle post creation
};

// Export the controller functions
module.exports = {
    renderCreatePostPage,
    createPost
};
